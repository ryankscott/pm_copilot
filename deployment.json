{
  "name": "deploy",
  "version": "1.0.0",
  "scripts": {
    "deploy:vercel": "npm run build && npx vercel --prod",
    "deploy:netlify": "npm run build && npx netlify deploy --prod --dir=dist",
    "setup:env": "cp .env.example .env.local && echo '✅ Environment file created. Please add your OpenAI API key.'",
    "setup:ai": "echo '🤖 AI Setup Instructions:' && echo '1. Get OpenAI API key from https://platform.openai.com/api-keys' && echo '2. Add OPENAI_API_KEY=your_key_here to .env.local' && echo '3. Deploy to Vercel for full AI functionality'"
  }
}
